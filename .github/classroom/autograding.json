{
  "tests": [
    {
      "name": "GatorGrader",
      "setup": "sudo pip install --upgrade pip && pip install pipenv && pipenv install --dev --three && pip install bs4 && pip install requests && source config/.pages.sh",
      "run": "gradle grade",
      "input": "",
      "output": "",
      "comparison": "included",
      "timeout": 10,
      "points": null
    }
  ]
}